const resNavBar = document.querySelector('i');

resNavBar.addEventListener('click', () => {
    let mobileMenu = document.querySelector('.main-menu');
    mobileMenu.classList.toggle('mobile-menu')

})